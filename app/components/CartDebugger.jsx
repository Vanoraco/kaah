/**
 * A component to display debug information about the cart
 * This component is completely disabled in production
 * @param {{
 *   cart: any;
 * }}
 */
export function CartDebugger({cart}) {
  // Return null to ensure no debug information is rendered
  return null;
}
